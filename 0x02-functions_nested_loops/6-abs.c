#include "main.h"
/**
 * _abs - check main
 * @r: integre input
 * this function return absloute value of a number
 * Return: Absolute value of number r
 */
int_abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}

